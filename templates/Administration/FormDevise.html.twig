{% extends 'admin.html.twig' %}

{% block title %}Devise{% endblock %}

{% block body %}

    <div id="layoutSidenav_content" class="mt-5">
        <main>
            <div class="container-fluid px-4">
                <div class="d-flex justify-content-between">
                    <h3 class="text-black-50"><i class="fad fa-badge-dollar"></i> DEVISE</h3>
                    <a href="{{ path("devise") }}" class="btn btn-dark">< Retour à la liste</a>
                </div>
                <div class="col-md-6 mt-5">
                    {{ form_start(FormDevise) }}
                    {{ form_row(FormDevise.DeviseValue) }}
                    <button type="submit" class="btn btn-primary"><i class="fad fa-check"></i> Enregistrer</button>
                    {{ form_end(FormDevise) }}
                </div>
            </div>
        </main>
    </div>

{% endblock %}
