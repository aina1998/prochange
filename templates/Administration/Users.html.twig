{% extends 'admin.html.twig' %}

{% block title %}Listes des utilisateurs{% endblock %}

{% block body %}

    <div id="layoutSidenav_content" class="mt-5">
        <main>
            <div class="container-fluid px-4">

                {% include "Partials/Flash.html.twig" %}

                <div class="d-flex justify-content-between">
                    <h3 class="text-black-50"><i class="fad fa-user-circle"></i> LISTE DES UTILISATEURS</h3>
                    <a href="{{ path("account-register") }}" class="btn btn-primary"><i class="fad fa-plus"></i> Ajouter un utilisateur</a>
                </div>

                <table class="table table-bordered mt-5">
                    <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Nom d'utilisateur</th>
                        <th scope="col">Adresse e-mail</th>
                        <th scope="col">Slug</th>
                        <th scope="col">Date d'enregistrement</th>
                        <th scope="col"></th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for listUser in listUsers %}
                        <tr>
                            <th scope="row">{{ listUser.id }}</th>
                            <td>{{ listUser.pseudo }}</td>
                            <td>{{ listUser.email }}</td>
                            <td>{{ listUser.slug }}</td>
                            <td>{{ listUser.createdAt | date("d/m/Y") }}</td>
                            <td class="text-center">
                                <a href="{{ path('edit-user', {'slug' : listUser.slug}) }}" class="btn btn-warning"><i class="fad fa-user-edit"></i></a> -
                                <a href="{{ path('delete-user', {'slug' : listUser.slug}) }}" class="btn btn-danger"><i class="fad fa-trash-alt"></i></a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </main>
    </div>

{% endblock %}
